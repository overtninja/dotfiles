[user]
	email = overtninja@users.noreply.github.com
	name = overtninja
	#signingkey =

#[gpg]
#    program = qubes-gpg-client-wrapper

#[commit]
#    gpgsign = true

[diff]
    tool = vimdiff

[merge]
    tool = vimdiff

[difftool]
    prompt = false

#[push]
#		gpgsign = true

[alias]
    pickle-that = "!git commit --amend -m \"$(git log --format=%s -n1) 🥒\" -m \"$(echo)$(git log --format=%b -n1)\""
    put-a-bird-on-it = "!git commit --amend -m \"$(git log --format=%s -n1) 🐦\" -m \"$(echo)$(git log --format=%b -n1)\""
